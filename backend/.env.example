# LLM Configuration (supports 'gemini' or 'openai')
LLM_PROVIDER=gemini
LLM_API_KEY=your_api_key_here
LLM_MODEL=gemini-1.5-flash

# Set DEMO_MODE to true to run with realistic heuristic demo analysis without requiring an API key
DEMO_MODE=true
